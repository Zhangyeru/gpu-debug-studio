# AI Coding 总结思考

这个项目从头到尾基本是跟 AI 结对编程完成的。整个过程有惊喜也有踩坑，简单记录一下。

最爽的是**从零到一的速度**。一个空目录，几句话描述需求，半小时内前后端架子就搭好了，React 组件、FastAPI 路由、CodeMirror 集成这些体力活全被承包了。如果纯手写，光配置文件和样板代码就得花半天。

但中间也反复折腾了不少次。比如 LLM 输出格式不稳定，有时候带 markdown 代码块，有时候行号格式不统一（`[第8行]` vs `[第1-5行]`），前端就得加正则容错。还有 CodeMirror 的 Decoration API，TypeScript 类型报了半天错，最后干脆用 DOM 操作绕过去了——简单粗暴但确实管用。

比较意外的是**改口的成本很低**。从"加个流程图"到"删掉流程图"，从"合并在一起"到"拆成两个标签页"，这种需求变化放在传统开发里可能要重构不少东西，但 AI 基本上几分钟就改完了，试错成本极低。

最后说点真实的：AI 写代码快，但**理解和调试还是得靠人**。它写的 CodeMirror 行高亮代码第一版根本不 work，Mermaid 渲染也挂了，排查还是得自己看文档、猜根因。AI 是个很好的加速器，但方向盘还得握在自己手里。

整体感觉，这种工具型小项目的开发模式已经变了——不再是写代码，更多是**描述需求和做决策**。代码本身变成了一种 commodity，真正的价值在于知道要做什么、怎么组合、什么是好的交互。
